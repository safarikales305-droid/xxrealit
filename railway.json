{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "cd nestjs-backend && npm install && npm run build"
    },
    "deploy": {
      "startCommand": "cd nestjs-backend && npm run start:prod"
    }
  }