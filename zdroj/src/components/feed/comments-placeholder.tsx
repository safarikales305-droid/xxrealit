/**
 * Shell for future comments UI — TikTok-style thread under the action column.
 */
export function CommentsPlaceholder() {
  return (
    <div className="mt-2 max-w-[4rem] text-center text-[10px] font-medium leading-tight text-white/55">
      Komentáře — připravujeme
    </div>
  );
}
